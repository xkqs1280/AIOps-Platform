[pytest]
# tests/ 为平台回归用例（async + SQLite 内存库）；
# backend/tests/ 为 P0/P1 专项回归（unittest 风格，需从仓库根运行）。
testpaths = tests backend/tests
asyncio_mode = auto
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
