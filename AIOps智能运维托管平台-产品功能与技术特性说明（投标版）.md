# AIOps 智能运维托管平台
## 产品功能与技术特性说明（投标版）

| 项目 | 内容 |
|---|---|
| 产品名称 | AIOps 智能运维托管平台 |
| 版本 | v4.0 |
| 文档用途 | 投标技术方案 / 产品功能说明 |
| 编制日期 | 2026 年 8 月 |

---

## 一、产品概述

**AIOps 智能运维托管平台**是一套面向网络及安全设备的 **7×24 小时一体化智能运维管理系统**，覆盖"**监控 → 告警 → 运维 → 巡检 → 安全 → 合规**"全流程运维闭环。平台将设备监控、故障告警、配置管理、专项巡检、业务监控、生命周期管理、安全态势与等保合规等功能整合于同一平台，帮助运维团队实现：

- **统一纳管**：多厂商、多类型网络与安全设备集中纳管，协议驱动自动发现；
- **主动预警**：规则驱动的智能告警引擎，设备故障第一时间感知，减少业务中断；
- **自动化运维**：配置备份、H3C 专项巡检、业务探测自动化，替代人工重复操作；
- **安全合规**：威胁情报联动、Syslog 归一化、等保 2.0 基线核查，满足监管要求；
- **轻量交付**：支持 Windows 单机一键部署与 Linux 分布式部署，**完全离线可用**，适配内网/涉密等隔离网络环境。

---

## 二、产品定位与价值

| 维度 | 平台价值 |
|---|---|
| **网络可观测性** | 设备存活、CPU/内存/温度/接口流量等核心指标实时采集，大屏直观呈现全网运行态势 |
| **故障快速定位** | 告警规则引擎 + SNMP Trap 双重感知，4 级告警分级，持续时长防抖动，杜绝误报刷屏 |
| **运维效率** | H3C 25 类专项巡检命令自动执行并生成 Word/Excel 报告，替代人工逐台登录 |
| **配置安全** | 华为/H3C 设备配置自动备份、版本对比、SHA-256 完整性校验，防配置丢失与篡改 |
| **安全合规** | 开放威胁情报实时预警 + 等保 2.0 二级/三级基线核查，一键生成不合规清单 |
| **投资保护** | 内网离线部署、跨平台（Windows/Linux）、无需专有硬件，存量设备直接纳管 |

---

## 三、核心功能模块

### 3.1 监控大屏（Dashboard）

- 全网设备总览：设备总数、在线/离线状态、活跃告警数，按厂商与设备类型分布统计；
- **CPU/内存占用 TOP5** 排行，快速定位高负载设备；
- **接口带宽利用率 TOP10**：基于真实 SNMP 接口双采样差分计算，反映实际流量占用；
- 设备生命周期提醒（保修到期/已过 EOL）与**最近 20 条活跃告警滚动展示**；
- 全屏大屏展示模式，适配监控中心/电视投屏场景。

### 3.2 设备管理（Devices）

- 设备台账 CRUD，支持分页多维筛选（厂商/类型/状态/关键词）、批量导入导出、批量删除，纳管数量可配置上限；
- **SNMP 自动发现与信息补全**：自动采集 sysDescr / sysName / 序列号等系统信息，自动识别**华为、H3C、Cisco、锐捷、深信服、Linux** 等厂商型号；
- 支持 **SNMPv2c**（基于 pysnmp 纯 Python 实现），GETBULK 优先、自动回退 GETNEXT，兼容性良好；
- 设备详情页：接口列表、实时接口流量 TOP、SNMP 信息一键同步刷新。

### 3.3 智能告警引擎（Alerts / AlertRules）

- **状态库规则引擎**：每 60 秒随采集循环自动评估一轮，规则可按需启停；
- 支持指标：**CPU 使用率、内存使用率、设备温度、系统重启（Uptime 跳变）、接口状态（UP→DOWN）**；
- 规则条件：大于/小于/等于/不等于等 6 种比较符，**持续时长达到阈值才触发**（有效防止瞬时抖动误报）；
- **四级告警分级**（严重/重要/次要/警告），触发后自动恢复；
- 按"设备 + 规则"自动去重，防止告警风暴刷屏；支持告警筛选、删除、一键清空、统计图表；
- **SNMP Trap 实时接收**：内置华为 MIB 全族映射（温度/电压/CPU/内存/单板/端口/堆叠/接口流量等），支持冷启动、链路 UP/DOWN、认证失败等标准 Trap，以及 BGP/OSPF/VRRP 路由协议事件，按来源 IP 自动关联设备并生成/恢复告警。

### 3.4 网络拓扑（Topology）

- 以已纳管设备为节点，节点呈现设备实时状态与 CPU/内存指标，自定义连线构建全网拓扑；
- 自动防自连、边去重、节点归一化，直观呈现设备间连接关系，辅助故障定位。

### 3.5 配置备份（ConfigBackup）

- 通过 **SSH/Telnet** 采集设备运行配置，兼容**华为 VRP / H3C Comware**（含老旧加密算法协商）；
- 自动处理分页、More 续传，提示符判定输出结束，**完整性校验**（缺 return / 分页截断判定备份失败）；
- 支持**手动备份、全部备份、定时备份**（每日/每周/每月）；
- 配置 **SHA-256 指纹**与版本 Diff 对比，配置变更可追踪、可审计。

### 3.6 H3C 专项巡检（Inspection）

- 内置 **25 类巡检命令**：版本、单板、CPU、内存、环境、风扇、电源、日志、BFD、MAC 漂移、链路聚合、光模块、计数器、接口状态、OSPF/BGP 等；
- **SSH 多会话并行分片采集**，重命令分散执行，2 台设备并发、任务级超时保护；
- 自动生成 **Excel 汇总表 + Word 巡检报告**，单台失败可重跑并重新生成报告，显著提升 H3C 设备定期巡检效率。

### 3.7 重要业务监控（BusinessMonitor）

- 业务分组 + 业务终端（IP/MAC）管理，**每 5 分钟 ICMP 探测**（3 次重试）；
- 连续 2 轮（约 10 分钟）离线才触发告警，防抖动；恢复自动通知；支持手动单台/全量探测；
- 面向关键业务链路的可用性保障。

### 3.8 设备生命周期（Lifecycle）

- 厂商型号 **EOS/EOL 库**维护（增删改查、搜索），可从已纳管设备批量回写型号信息；
- 提醒分级：**保修到期 / EOS 临近 / EOL 临近 / 紧急 / 已过 EOL**；
- 内置华为、H3C、深信服等主流厂商种子数据，设备淘汰与维保到期自动预警。

### 3.9 安全监控（Security）

- 集成 **FireHOL 三大类开放威胁情报**（高危来源 IP / 24 小时滥用源 / 匿名代理）与 **ipwho.is 地理定位**，每 30 分钟自动快照；
- 呈现攻击类型分布、来源国家 TOP10、中国来源数量、历史趋势；
- 内置 **Syslog 归一化引擎**：厂商识别、severity/action 标准化为统一安全事件，与外部威胁情报联动研判。

### 3.10 等保合规（Compliance）

- 内置**等保 2.0 三级规则集**（8.1.3/8.1.4/8.1.5 控制点，按设备类型自动匹配）；
- **等保二级 SSH 配置核查 14 项**：身份鉴别、访问控制、安全审计、入侵防范、数据保密性；
- 状态分级（符合/部分符合/不符合）与**百分制评分**，输出全局概要、不合规清单；SSH 不可用时自动回退 SNMP 指标推断，核查不中断。

### 3.11 账号与权限（Account / Auth）

- **三角色权限模型**：admin（管理员）、operator（运维）、viewer（只读），角色级操作鉴权；
- 基于 **JWT（HS256）+ HttpOnly Cookie** 的无状态认证，密码 bcrypt 加密存储并强制 12 位强度校验；
- 设备凭据（SNMP 团体名/管理密码）采用 **Fernet 对称加密**存储，数据库泄露不暴露明文；
- 支持用户管理、密码修改、退出登录；授权到期平台自动锁定仅保留授权页（可选开关）。

---

## 四、技术架构

```
┌─────────────────────────────────────────────────┐
│              前端（Vue 3 + Tailwind CSS）          │
│        监控大屏 / 设备 / 告警 / 拓扑 / 巡检 / 安全…  │
└──────────────────────┬──────────────────────────┘
                       │ HTTP + JWT Cookie
┌──────────────────────▼──────────────────────────┐
│         后端服务层（FastAPI + Uvicorn）            │
│  设备/告警/拓扑/备份/巡检/业务/生命周期/安全/合规/授权 │
├─────────────────────────────────────────────────┤
│  采集引擎（pysnmp6 / asyncssh / telnetlib3）      │
│  SNMP 采集 · Trap 接收 · SSH/Telnet 巡检 · 业务探测│
├─────────────────────────────────────────────────┤
│  调度与处理（APScheduler · 状态库规则引擎）         │
│  数据加工（pandas/openpyxl/python-docx 报告）     │
├─────────────────────────────────────────────────┤
│             PostgreSQL 16（数据持久化）            │
└─────────────────────────────────────────────────┘
```

**核心组件说明**

| 组件 | 选型 | 说明 |
|---|---|---|
| 后端框架 | Python 3.10+ / FastAPI / Uvicorn | 高性能异步框架，自动生成 OpenAPI 文档 |
| ORM | SQLAlchemy 2.0（异步）+ psycopg | 异步数据库访问，支持 PostgreSQL |
| 数据校验 | Pydantic v2 | 请求/响应强类型校验 |
| SNMP | pysnmp 6（纯 Python） | **跨平台、零外部依赖**，无需安装 Net-SNMP |
| SSH/Telnet | asyncssh / telnetlib3 | 异步协议采集，兼容老旧加密算法 |
| 认证加密 | python-jose / passlib(bcrypt) / cryptography | JWT、密码哈希、凭据加密 |
| 报表 | openpyxl / python-docx / pandas | Excel / Word 巡检与合规报告 |
| 定时调度 | APScheduler | 采集、巡检、备份、威胁情报快照等任务调度 |

---

## 五、技术特性与优势

1. **全链路协议自研、零外部依赖**
   SNMP 采集（pysnmp）、SSH/Telnet 配置采集与巡检均内置实现，**不需要在目标服务器安装任何 SNMP 命令行工具**，降低部署复杂度与安全风险。

2. **完全离线可用**
   平台本体与全部核心功能在内网/隔离网环境可正常运行（仅外部威胁情报模块需要联网，可配置关闭），符合涉密及隔离网络要求。

3. **跨平台、轻量交付**
   - **Windows**：PyInstaller 单文件打包，一键部署脚本自动安装 PostgreSQL 并启动服务，支持开机自启与 Windows 服务注册；
   - **Linux**：源码部署 + 一键安装脚本（自动建虚拟环境、装依赖、准备数据库）；
   - **Docker**：Compose 一键编排（PostgreSQL 16 + 平台容器）。
   - 升级仅需替换后端与前端产物，**数据库表结构自动同步**（init_db），无需人工迁移。

4. **单端口一体化**
   Windows 生产包由后端同端口托管前端，**单进程、单端口（8000）** 即可对外服务，无需 Nginx 等额外组件，安全加固面小。

5. **告警精准防误报**
   持续时长判定 + 去重机制，有效避免网络抖动造成的告警轰炸；支持一键清空与分级统计。

6. **报告自动化**
   H3C 巡检自动生成 Word/Excel 报告、等保合规自动输出核查清单，开箱即用。

7. **数据安全**
   设备凭据加密存储、JWT 无状态会话、HttpOnly Cookie、安全响应头（X-Frame-Options / X-Content-Type-Options / Referrer-Policy / Permissions-Policy）全面加固。

8. **预测能力预留**
   集成 scikit-learn 数据处理与预测能力，可扩展 CPU/内存等指标的容量趋势预测。

---

## 六、兼容性

| 维度 | 支持范围 |
|---|---|
| 操作系统 | Windows Server 2016+ / Windows 10+；Ubuntu 22.04+ / Debian 12+ / CentOS 9+ |
| 运行环境 | Python 3.10+（推荐 3.12） |
| 数据库 | PostgreSQL 16+（默认端口 5432） |
| 网络设备厂商 | 华为、H3C、Cisco、锐捷、深信服、Linux 服务器等（SNMPv2c 均可纳管） |
| 浏览器 | Chrome / Edge 等现代浏览器（移动端自适应待扩展） |
| SNMP 协议 | SNMPv2c，GETBULK/GETNEXT 自适应回退 |
| 管理协议 | SSH（含老旧算法）、Telnet |
| 部署形态 | 单机一体化、分布式、Docker 容器化 |

---

## 七、性能与可靠性

- **健康检测**：设备存活每 5 秒 SNMP 探测，连续 3 次无响应判定离线（约 15 秒感知）；
- **采集并发**：接口流量采样并发限流保护，避免过度占用设备资源；
- **任务保护**：巡检任务级超时、采集完整性校验，失败可重跑；
- **数据库连接池**：后端启用连接池（pool_size/max_overflow）应对并发查询；
- **会话安全**：授权到期锁定（可选）、JWT 过期自动跳登录、Cookie 安全标记。

---

## 八、授权与交付

- **授权模式**：离线激活码授权（RSA 签名 + 机器指纹绑定），分 **测试版（3 个月全功能）** 与 **全功能版（永久）** 两种；激活码离线验签，**完全无需联网**；
- **交付形态**：
  - Windows 生产包（zip，含一键部署脚本与 PostgreSQL 安装器）；
  - Linux 源码包（zip，含一键安装脚本）；
  - Docker 镜像（可选）；
- **厂商授权工具**：配套「AIOPS 激活码生成工具」，支持按机器码生成激活码、授权记录管理。

---

## 九、服务与支持

- 部署支持：Windows/Linux 部署指导文档与一键脚本，复杂环境远程协助；
- 升级支持：版本升级包与升级说明，数据库自动同步，历史数据无损保留；
- 定制开发：支持按项目需求扩展监控指标、巡检命令、合规规则、报表模板与对接第三方平台；
- 培训交付：操作手册、管理员培训、验收测试支持。

---

*© 2026 AIOps Platform v4.0*
